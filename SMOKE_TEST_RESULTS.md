# Smoke Test Results

## 2026-05-31 17:50 UTC - QA smoke round #1

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback.
- Environment: reused existing CRA frontend `http://127.0.0.1:3000` and backend `http://127.0.0.1:8081`; backend health returned `200 {"status":"UP","groups":["liveness","readiness"]}`.
- API handling: Playwright route-fulfilled local `/api/*` requests by proxying to the live backend without changing product code, avoiding the known local CRA `/api` proxy/CORS limitation.
- Coverage: desktop `1366x900` home, product list, keyword search, product detail, guest add-to-cart, cart, checkout entry, login, register, forgot password, coupons, order tracking, profile login intercept, admin dashboard login intercept.
- Coverage: mobile `390x844` home, product list, keyword search, product detail, cart, checkout entry, login, coupons, order tracking, profile login intercept.
- Result: PASS for core storefront smoke. No CRA overlay, no page errors, no horizontal overflow on checked desktop/mobile pages, product list/detail loaded live `PawPilot` data, guest cart contained `PawPilot` after add-to-cart, checkout entry exposed payment/address flow, `/profile` redirected to `/login`, and `/admin/dashboard` redirected to login when unauthenticated.
- F404 retest evidence: product list requests were `GET /products?sort=default&page=0&size=12` and `GET /products?keyword=feeder&sort=default&page=0&size=12`; no default `minPrice=0` or `maxPrice=0` params were sent.
- Live API statuses observed: `200 /api/app/config`, `200 /api/announcements/active?limit=4`, `200 /api/products/featured?limit=12`, `200 /api/products?page=0&size=48`, `200 /api/categories?level=1`, `200 /api/pet-gallery`, `200 /api/products?sort=default&page=0&size=12`, `200 /api/products?keyword=feeder&sort=default&page=0&size=12`, `200 /api/products/1`, `200 /api/reviews/product/1`, `200 /api/product-questions/product/1`, `200 /api/products/1/recommendations`, `200 /api/products/by-ids?ids=1`, `200 /api/payments/channels`, `200 /api/coupons/public`.
- New issue: added F406 OPEN because live `GET /announcements/active?limit=4` returns active customer-visible test/gibberish content (`TEST fast` / `sadsadsafffffqewqe1231413214132141231`), and the navbar repeats it across storefront pages.
- Non-issue notes: Ant Design deprecation warnings, React Router future-flag warnings, and React `fetchPriority` dev warning were observed in console but did not break smoke; not recorded as product defects in this round. `net::ERR_ABORTED` lazy chunk requests were caused by deliberate fast navigation between routes, and Unsplash ORB image-load noise did not affect DOM smoke; no image capture or image artifact was produced.
- Environment limitation: no admin test credentials were available, so post-login admin pages were not covered in this round; unauthenticated admin login interception was covered.

## 2026-05-31 18:10 UTC - Alert center F160/F192 no-media regression

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback.
- Environment: reused existing CRA frontend `http://127.0.0.1:3000`; no services were started or stopped. Live backend admin alert endpoints still returned `401` without a matching runtime admin token, so authenticated Alert Center DOM checks used Playwright route fixtures and backend filter semantics were verified from current source.
- Coverage: `/admin/alerts` authenticated browser DOM; default status scope, summary-card click/keyboard behavior, request query parameters, active-card state, and alert row text. Fixture data included one `OPEN/CRITICAL/DATABASE`, one `ACKNOWLEDGED/WARNING/APPLICATION`, and one `RESOLVED/INFO/RATE_LIMIT` alert.
- F160 result: PASS in authenticated DOM regression. Initial load had status selector `ALL`, row count `3`, mixed-status rows visible, and request `GET /admin/alerts?limit=300` with no `status`, `severity`, or `category` filter. Summary counts displayed `Open 1`, `Acknowledged 1`, and `Resolved 1`, matching the visible default all-status list.
- F160 interaction evidence: clicking the Open statistic card sent `GET /admin/alerts?status=OPEN&limit=300`, made `Open 1` active, selected `OPEN`, and showed only the open row. Focusing the Acknowledged card and pressing Enter sent `GET /admin/alerts?status=ACKNOWLEDGED&limit=300`, made `Acknowledged 1` active, selected `ACKNOWLEDGED`, and showed only the acknowledged row. Focusing the Resolved card and pressing Space sent `GET /admin/alerts?status=RESOLVED&limit=300`, made `Resolved 1` active, selected `RESOLVED`, and showed only the resolved row. All three statistic cards exposed `tabIndex="0"`.
- F192 result: PASS by current source review. `SystemAlertService.search(...)` now uses `normalizeSeverityFilter(...)` and `normalizeCategoryFilter(...)`; null, blank, and `ALL` return `null`, and the SQL predicates `(? IS NULL OR severity = ?)` / `(? IS NULL OR category = ?)` keep those filters as wildcards instead of converting them to `WARNING` / `APPLICATION`.
- Observed console/page state: no page errors and no CRA overlay. One Ant Design Spin warning was observed and treated as unrelated noise.
- Result: PASS for F160 DOM regression and F192 source-level filter regression; no new issue ID.

## 2026-05-31 18:16 UTC - F406 active announcement no-media regression

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback.
- Environment: reused existing CRA frontend `http://127.0.0.1:3000` and backend `http://127.0.0.1:8081`; backend health returned HTTP 200. Browser checks route-proxied local `/api/*` to the live backend because the local CRA dev server returns `index.html` for direct `/api/*` requests. Image resource requests were aborted to avoid image collection; DOM/API/text evidence was still collected. A post-run artifact check found no new `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.zip`, `.trace`, `.webm`, or `.mp4` files under `/home/guhao/shoptest` since `2026-05-31 18:10 UTC`.
- Direct API evidence: `GET http://127.0.0.1:8081/announcements/active?limit=4` returned HTTP 200 JSON with `{"id":1,"title":"TEST fast","content":"sadsadsafffffqewqe1231413214132141231","linkUrl":"https://pet.686888666.xyz/products/3"}`. The response still contains both the QA/test title and the gibberish content.
- DOM coverage: desktop `1366x900` and mobile `390x844` storefront pages `/`, `/products`, `/products/1`, `/cart`, `/checkout`, `/coupons`, and `/track-order`.
- DOM evidence: every covered page loaded `200 /api/announcements/active?limit=4` and the `.shop-nav__ticker` text contained repeated `sadsadsafffffqewqe1231413214132141231` entries linked to `https://pet.686888666.xyz/products/3`. `bodyHasGibberish=true` and `tickerHasBadText=true` were observed on all 14 desktop/mobile route checks.
- Layout/runtime notes: no CRA overlay, no page errors, and no horizontal overflow were observed on the covered routes. Console noise included known Ant Design/React Router warnings plus `ERR_BLOCKED_BY_CLIENT` entries caused by the deliberate image-request block.
- Source/runtime comparison: current `SiteAnnouncementService.java` source contains the expected public display guard and ACTIVE save/update rejection for placeholder or gibberish copy, but the currently running backend on `8081` still serves the old bad active announcement. This points to a stale runtime/deploy/restart gap in the current test environment, while the customer-visible smoke result is still failing.
- Result: FAIL. F406 is not verified fixed in the current running environment; no new issue ID was added, and the existing F406 record was updated with this regression evidence.

## 2026-05-31 18:37 UTC - F406 startup-cleanup no-media regression against live 8081

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback, and no backend/frontend processes were killed or restarted.
- Environment: reused existing CRA frontend `http://127.0.0.1:3000` and backend `http://127.0.0.1:8081`; backend health returned HTTP 200. Browser checks route-proxied local `/api/*` to live `8081` because the local CRA dev server returns `index.html` for direct `/api/*` requests. Image resource requests were aborted to avoid image collection; DOM/API/text evidence was still collected.
- Source/deploy check: current source contains `ApplicationReadyEvent` cleanup in `SiteAnnouncementService.deactivatePlaceholderActiveAnnouncements()` and `SiteAnnouncementRepository.findByStatusIgnoreCase(...)`. The running deployed artifact is stale: `/opt/shoptest/shop.jar` timestamp is `2026-05-30 14:03:19 +0000`, and `strings` on `BOOT-INF/classes/com/example/shop/service/SiteAnnouncementService.class` did not find `Deactivated`, `findByStatusIgnoreCase`, `ApplicationReadyEvent`, or the ACTIVE placeholder rejection text. The Java process on `8081` started at `2026-05-31 18:30:02 UTC`, but it is running that stale jar.
- Direct API evidence: `GET http://127.0.0.1:8081/announcements/active?limit=4` returned HTTP 200 JSON with `{"id":1,"title":"TEST fast","content":"sadsadsafffffqewqe1231413214132141231","linkUrl":"https://pet.686888666.xyz/products/3"}`. The response still contains the known QA/test title and gibberish content.
- DOM coverage: desktop `1366x900` and mobile `390x844` storefront pages `/`, `/products`, `/products/1`, `/cart`, `/checkout`, `/coupons`, and `/track-order`.
- DOM evidence: every covered page loaded `200 /api/announcements/active?limit=4`; `.shop-nav__ticker` contained repeated `sadsadsafffffqewqe1231413214132141231` entries linked to `https://pet.686888666.xyz/products/3`; `tickerHasBadText=true` and `bodyHasGibberish=true` were observed on all 14 desktop/mobile route checks.
- Runtime notes: no CRA overlay, no page errors, and no horizontal overflow were observed. Console noise was limited to known Ant Design/React Router/React dev warnings. A post-run artifact check found no new `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.zip`, `.trace`, `.webm`, or `.mp4` files under `/home/guhao/shoptest` since `2026-05-31 18:31 UTC`.
- Result: BLOCKED/FAIL for live F406 verification because the active service is running a stale deployed jar, not the current source with startup cleanup. This is not a new product issue; F406 remains OPEN until `8081` is rebuilt/redeployed with the current source and the no-media API/DOM regression passes.

## 2026-05-31 18:44 UTC - F339 Nginx security-header no-media regression

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback, and no Nginx reload/restart was performed.
- Environment: local Nginx on ports `80` and `443`, tested with `curl -I --resolve pet.686888666.xyz:{80,443}:127.0.0.1`. Active Nginx config is `/etc/nginx/sites-enabled/shoptest` -> `/etc/nginx/sites-available/shoptest`, modified `2026-05-30 02:32:59 +0000`; it is not the updated repo config in `deploy/nginx/shoptest-static.conf` / `deploy/nginx/shoptest-edge.conf.template`.
- Static/deploy comparison: repo config has repeated security headers in each Cache-Control child block, but `nginx -T` for the active config still shows child blocks such as `/healthz`, `@spa_not_found`, `/static/`, `/runtime-config.js`, generic asset regex, and `/uploads/` declaring `add_header Cache-Control` without repeating the full security header set.
- Header evidence: HTTP `/healthz` returned `301` with no `Cache-Control` or security headers. HTTPS `/healthz` returned `204` with `Cache-Control: no-store` but missing `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `Strict-Transport-Security`, and `Content-Security-Policy`.
- Header evidence: HTTPS `/static/css/main.24899161.css`, `/runtime-config.js`, `/favicon.ico`, `/uploads/f339-header-probe.jpg`, and `/static/js/main.6f32d2b3.js.map` returned Cache-Control but missed the expected security headers. HTTPS `/not-a-real-route` returned security headers but missed `Cache-Control`.
- Result: BLOCKED/FAIL for live F339 verification because Nginx has not loaded the fixed repo config. This is not a new product issue; F339 remains source-fixed but requires deploy/reload of the updated Nginx config before live header regression can pass.

## 2026-05-31 18:47 UTC - F343 public catalog pagination no-media regression

- Media capture: 未截图、未录屏、未生成 trace/图片.
- Agent boundary: QA-only run; no product code changed, no commits, no rollback, and no backend/frontend processes were killed or restarted.
- Source evidence: current `ProductController` sets omitted public `/products` pagination to `page=0,size=24`, calls `productService.findPublicProductPage(query)`, and returns `ProductPublicPageResponse.of(...)`. Current `Home.tsx` requests `productApi.getAll(..., { page: 0, size: HOME_PRODUCT_PAGE_SIZE })`; current `ProductList.tsx` uses `productApi.getPage(...)` with `page`/`size`; `productApi.getAll(...)` accepts legacy arrays or page responses and normalizes them for existing callers.
- Live backend evidence: current `http://127.0.0.1:8081` is still backed by stale `/opt/shoptest/shop.jar`. `strings` on `BOOT-INF/classes/com/example/shop/controller/ProductController.class` did not find `ProductPublicPageResponse` or `findPublicProductPage`.
- Direct API evidence: `GET http://127.0.0.1:8081/products` returned HTTP 200 legacy array with `itemCount=29`, no `total`, `page`, or `size` metadata. `GET http://127.0.0.1:8081/products?page=0&size=24` returned HTTP 200 legacy array with `itemCount=24`, also with no pagination metadata.
- Frontend DOM/network evidence: using existing CRA `http://127.0.0.1:3000` with local `/api/*` route-proxied to live `8081`, ProductList `/products` rendered recognizable product text (`PawPilot`, `HydraWhisk`, `CloudNap`, `TrailTails`) and sent bounded `GET /api/products?sort=default&page=0&size=12`. Home `/` rendered product text after scroll/wait and sent bounded `GET /api/products?page=0&size=48` plus `GET /api/products/featured?limit=12`.
- Runtime notes: no CRA overlay and no page errors were observed. Images were aborted to avoid image collection. A post-run artifact check found no new `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.zip`, `.trace`, `.webm`, or `.mp4` files under `/home/guhao/shoptest` since `2026-05-31 18:43 UTC`.
- Result: PARTIAL PASS / BLOCKED. Current source and frontend request shape match the F343 fix, and Home/ProductList render with bounded requests. Live backend verification of `/products` default pagination metadata is blocked/failing because `8081` is still running a stale jar; no new issue ID was added.
